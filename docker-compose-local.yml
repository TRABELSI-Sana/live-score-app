services:
  redis:
    image: redis:7-alpine
    ports:
      - "127.0.0.1:6379:6379"
  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
